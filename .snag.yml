---
# Snag configuartion

verbose: true

ignore:
  - .git
  - sample/_gomutate
  - "**.out"
  - gomutate

build:
  - go build ./cmd/gomutate
  - go vet .
  - gofmt -l -s .
  - golint .
  - go test . ./cmd/... ./mutants
